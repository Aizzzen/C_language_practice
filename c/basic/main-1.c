#include <stdio.h>
#include <stdbool.h>

int main() {
    // const int code = 13; // const

    // int * const ptr_arr; // константный указатель

    // // нельзя менять значения
    // int a[6] = {1, 2, 3, 4, 5, -1};
    // const int *ptr_arr;
    // ptr_arr = a;

    // for(int i = 0; i < sizeof(a) / sizeof(*a); i++) {
    //     printf("%d", ptr_arr[i]);
    // }

    // int n;
    // bool is_n;
    // if(scanf("%d", &n) != 1) {
    //     is_n = false;
    // } else {
    //     is_n = true;
    // }

    // printf("%d", is_n); // 0/1

    // int count_1 = 0;
    // int count_2 = 5;

    // int width = count_1++;
    // int height = ++count_2;
    // printf("%d\n%d", width, height); //0, 6

    // char byte_memory;
    // byte_memory = 100;
    // char a;
    // a = byte_memory;

    // char *ptr_to_b = &byte_memory;
    // char *ptr_to_a = &a;

    // printf("%s\n", ptr_to_b);
    // printf("%s", ptr_to_a);

    // int n = 10;
    // printf("%ld", sizeof(n));
    return 0;
}
